# Environment
ENVIRONMENT=development

# ============================================
# LOCAL DEVELOPMENT CONFIGURATION
# ============================================

# Firebase Admin SDK - Using JSON file path (easier for local dev)
# Download your service account JSON from Firebase Console → Project Settings → Service Accounts
# Save it to backend/ directory and update the path below
GOOGLE_APPLICATION_CREDENTIALS=./your-project-firebase-adminsdk-xxxxx.json

# Firebase Storage
# Format: your-project-id.firebasestorage.app OR your-project-id.appspot.com
FIREBASE_STORAGE_BUCKET=your-project-id.firebasestorage.app

# OpenRouter API (replaces direct Anthropic API)
# Get from: https://openrouter.ai/keys
OPENROUTER_API_KEY=sk-or-v1-xxxxx...
# OpenRouter base URL
OPENROUTER_BASE_URL=https://openrouter.ai/api/v1

# Email Service (Resend)
# Get from: https://resend.com/api-keys
RESEND_API_KEY=re_xxxxx...

# CORS (Frontend URLs - comma separated)
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000

# API Base URL
API_BASE_URL=http://localhost:8000

# ============================================
# CLOUD RUN DEPLOYMENT CONFIGURATION
# ============================================
# When deploying to Cloud Run, set these environment variables:
#
# Required:
#   ENVIRONMENT=production
#   FIREBASE_STORAGE_BUCKET=your-project-id.firebasestorage.app
#   OPENROUTER_API_KEY=sk-or-v1-xxxxx...
#   OPENROUTER_BASE_URL=https://openrouter.ai/api/v1
#   RESEND_API_KEY=re_xxxxx...
#   ALLOWED_ORIGINS=https://ltad-coach.vercel.app,https://your-custom-domain.com
#   API_BASE_URL=https://your-cloud-run-url.run.app
#
# Firebase credentials:
#   Cloud Run automatically uses Application Default Credentials (ADC)
#   No need to set GOOGLE_APPLICATION_CREDENTIALS or individual Firebase env vars
#   The Cloud Run service account will automatically have access to Firebase
#
# Deploy command:
#   gcloud run deploy ltad-coach-api \
#     --source . \
#     --region us-central1 \
#     --allow-unauthenticated \
#     --memory 2Gi \
#     --timeout 300s \
#     --set-env-vars ENVIRONMENT=production,FIREBASE_STORAGE_BUCKET=your-bucket \
#     --set-secrets OPENROUTER_API_KEY=openrouter-key:latest,RESEND_API_KEY=resend-key:latest
#
# See CLOUD_RUN_DEPLOYMENT.md for detailed deployment instructions
